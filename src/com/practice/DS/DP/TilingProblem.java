package com.practice.DS.DP;

public class TilingProblem {

	public static void main(String[] args) {
		/*
		 * Fibonacci
		 * If first tile is arranged horizontal -> next will also be horizontal so total number of ways count(n-2)
		 * If first tile is vertical -> total number of ways count(n-1)
		 * so total = count(n-1)+count(n-2)
		 */
	}

}
